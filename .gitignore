.DS_Store
*.xcuserstate
DerivedData/
*.xcresult
SwiftFormatRuleStudioCore/.build/
SwiftFormatRuleStudioCore/.build-coverage/
SwiftFormatRuleStudioCore/.swiftpm/
.swiftformat.*.backup
.swiftlint.yml.*.backup

# The .xcodeproj is committed (native sync-folder project). Ignore only the
# per-user state inside it.
**/xcuserdata/
.build/

# Tuist leftover (regenerated build artifact; project now generates Info.plist)
Derived/

# swift-infer working state (SemanticIndex, verify evidence, and the SwiftPM
# workdir it builds stubs in — the last runs to hundreds of MB). Machine-local:
# the evidence records verdicts measured against this checkout's source.
.swiftinfer/
